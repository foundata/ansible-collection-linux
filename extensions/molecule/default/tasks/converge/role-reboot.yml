# Tasks for the Molecule converge stage: Execute the collection role "reboot"
# against all driver-created instances.

- name: "Molecule | Converge | Include the foundata.linux.reboot role"
  ansible.builtin.include_role:
    name: "foundata.linux.reboot"
