---

security_fixes:
  - >
    ``user`` role - ``user_linux_accounts[].password.hash`` is now marked
    ``no_log`` in the argument specification, so a failing argument validation
    can no longer expose the password hash.
