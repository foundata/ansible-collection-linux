# Main entry point for the role.

---

- name: "Main | Include role initialization tasks"
  ansible.builtin.include_tasks:
    file: "{{ role_path }}/tasks/init.yml"
    apply:
      tags:
        - "always"
        - "sudo_linux_always"
  tags:
    - "always"
    - "sudo_linux_always"


# Separate includes for platform-specific files and default.yml on purpose. When
# looping over "platform files + ['default.yml']" for multiple platforms in one
# play, Ansible batches them across all hosts. Insertion order depends on when
# results arrive (non-deterministic behavior) and default.yml may run before the
# platform-specific files. Separate tasks enforce the required barrier.

- name: "Main | Include setup tasks (install): best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __sudo_linux_task_file }}"
    apply:
      tags:
        - "sudo_linux_setup"
  vars:
    __sudo_linux_task_file: "{{ role_path }}/tasks/setup/install/{{ __sudo_linux_loop_item }}"
  when:
    - sudo_linux_state == 'present'
    - __sudo_linux_task_file is ansible.builtin.file
  loop: "{{ __sudo_linux_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__sudo_linux_loop_item"
  tags:
    - "sudo_linux_setup"


- name: "Main | Include setup tasks (install): default"
  ansible.builtin.include_tasks:
    file: "{{ __sudo_linux_task_file }}"
    apply:
      tags:
        - "sudo_linux_setup"
  vars:
    __sudo_linux_task_file: "{{ role_path }}/tasks/setup/install/default.yml"
  when:
    - sudo_linux_state == 'present'
    - __sudo_linux_task_file is ansible.builtin.file
  tags:
    - "sudo_linux_setup"


- name: "Main | Include configuration tasks: best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __sudo_linux_task_file }}"
    apply:
      tags:
        - "sudo_linux_config"
  vars:
    __sudo_linux_task_file: "{{ role_path }}/tasks/config/{{ __sudo_linux_loop_item }}"
  when:
    - sudo_linux_state == 'present'
    - __sudo_linux_task_file is ansible.builtin.file
  loop: "{{ __sudo_linux_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__sudo_linux_loop_item"
  tags:
    - "sudo_linux_config"


- name: "Main | Include configuration tasks: default"
  ansible.builtin.include_tasks:
    file: "{{ __sudo_linux_task_file }}"
    apply:
      tags:
        - "sudo_linux_config"
  vars:
    __sudo_linux_task_file: "{{ role_path }}/tasks/config/default.yml"
  when:
    - sudo_linux_state == 'present'
    - __sudo_linux_task_file is ansible.builtin.file
  tags:
    - "sudo_linux_config"


- name: "Main | Include setup tasks (uninstall): best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __sudo_linux_task_file }}"
    apply:
      tags:
        - "sudo_linux_setup"
  vars:
    __sudo_linux_task_file: "{{ role_path }}/tasks/setup/uninstall/{{ __sudo_linux_loop_item }}"
  when:
    - sudo_linux_state == 'absent'
    - __sudo_linux_task_file is ansible.builtin.file
  loop: "{{ __sudo_linux_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__sudo_linux_loop_item"
  tags:
    - "sudo_linux_setup"

- name: "Main | Include setup tasks (uninstall): default"
  ansible.builtin.include_tasks:
    file: "{{ __sudo_linux_task_file }}"
    apply:
      tags:
        - "sudo_linux_setup"
  vars:
    __sudo_linux_task_file: "{{ role_path }}/tasks/setup/uninstall/default.yml"
  when:
    - sudo_linux_state == 'absent'
    - __sudo_linux_task_file is ansible.builtin.file
  tags:
    - "sudo_linux_setup"
